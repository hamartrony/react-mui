import { BlueBox } from "../../components/blue-box/blueBox";
import { LayoutPages } from "../../layouts/LayoutPages";

export const Login = () => {
  return (
    <LayoutPages>
      <BlueBox>Login</BlueBox>
    </LayoutPages>
  );
};
